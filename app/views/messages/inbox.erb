<%= link_to "Send A New Message", new_message_path%>
<h2>Inbox</h2>
<ul>
  <% @users_corresponded_with.each do |user| %>
    <li> <span class="number-bubble"><%=current_user.message_notifications_with(user).count%></span> <%= link_to user.username, "/messages/#{user.id}" %> </li>
  <% end %>
</ul>
